import React, { useLayoutEffect, useState } from "react";
import { colorStyle } from "../lib/data/styleData";
import { FlexBox } from "./FlexBox";

export const GetInfo = ({ length, width, n }) => {
  return <div>getinfo</div>;
};
